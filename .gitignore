\#*
*~
.#*
.DS_Store
.idea
.localeapp/locales
.project
.vscode
coverage
default
Gemfile.lock
tmp
nbproject
pkg
*.sw?
spec/dummy
.rvmrc
.sass-cache
public/spree
.ruby-version
.ruby-gemset
*.gem
*/*.gem

# Real Uber Direct Sandbox credentials for manual verification, never committed.
.env.sandbox

# The dummy app's test-only Active Record Encryption keys (spec_helper loads them with
# dotenv). Kept local like every other extension gem's; CI generates its own per run.
.env
